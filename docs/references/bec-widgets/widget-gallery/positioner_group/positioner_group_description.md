PositionerGroup creates a grid of positioner controls. Use it when a workflow needs several motors available together without adding a separate dock for each motor.

The device list is supplied as one space-separated string.
