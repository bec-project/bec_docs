# Python API

The API reference is allowed to pull objects from multiple sibling repositories even though the authored documentation is presented as one unified site.

The goal here is not to mirror repository structure in navigation, but to expose the important technical surfaces in a way that matches how readers reason about the system:

- BEC core
- GUI
- Devices
