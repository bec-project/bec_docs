# Run a Scan Through the GUI and the CLI

This tutorial will compare the scan workflow in the GUI and the CLI.

Placeholder focus:

- selecting a simple scan
- running it from the GUI
- running the equivalent command in the CLI
- checking the resulting output and state transitions
